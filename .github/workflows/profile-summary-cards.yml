name: GitHub-Profile-Summary-Cards

on:
  schedule: # execute every 24 hours
    - cron: "* */24 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: generate

    steps:
      - uses: actions/checkout@v3
        with:
          ref: "cards"
      - uses: vn7n24fzkq/github-profile-summary-cards@151a698a5112f4b50b5869fe3f923b9a1a33994d
        env:
          GITHUB_TOKEN: ${{ secrets.STATS_TOKEN }}
        with:
          USERNAME: ${{ github.repository_owner }}
          BRANCH_NAME: "cards"
          UTC_OFFSET: 9